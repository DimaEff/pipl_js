import React from 'react';
import styles from "./Settings.module";


const Settings = () => {
    return (
        <styles.Settings>
            Settings
        </styles.Settings>
    );
};

export default Settings;